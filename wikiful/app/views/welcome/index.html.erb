<h2 id="title">Welcome to Wikiful, where we drop knowledge bombs like it's WW3. Click below for the most recent 5 articles:</h2>
<div id="whatup">
	<% @recent_articles.each do |article| %>
	  <div>
	    <h3><%= link_to article.title, article %></h3>
	          <p>Published on <%= article.created_at.strftime('%b %d, %Y') %></p>
	    <p>Filed under: <% article.categories.each do |category| %>
	                      <%= link_to category.name, category %>&nbsp;
	                    <% end %>
	    </p>
	    <p><%= truncate(article.content, length: 200) %></p>
	  </div>
	<% end %>
</div>

<button type="button" class="boomButton btn btn-primary btn-sm btn-block">BOOM! Show me a few articles</button>